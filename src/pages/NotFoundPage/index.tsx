import { NotFound } from '@shared/ui/NotFound';
import { Layout } from '@shared/ui/Layout';

export const NotFoundPage = () => {
    return (
        <Layout>
            <NotFound />
        </Layout>
    );
};
